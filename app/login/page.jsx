import {createInvoice,getPasswordList} from '../lib/actions'
export default async function Page() {
  // const res= await createInvoice()
  // const res= await getPasswordList()
    return (
      <div className="h-full">
          {/* 密码{res} */}
      </div>
    );
  }